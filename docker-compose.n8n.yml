services:
  n8n:
    image: n8nio/n8n:latest
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - EXECUTIONS_DATA_SAVE_ON_SUCCESS=none
      - EXECUTIONS_DATA_SAVE_ON_ERROR=all
      - EXECUTIONS_DATA_PRUNE=true
      - EXECUTIONS_DATA_MAX_AGE=24
      - IXI_O_AGENT_INGEST_URL=http://host.docker.internal:3000/api/ingest/channel-talk
      - IXI_O_AGENT_OPENAPI_INGEST_URL=http://host.docker.internal:3000/api/ingest/channel-talk/openapi
      - IXI_O_AGENT_BACKFILL_URL=http://host.docker.internal:3000/api/backfill/channel-talk
      - IXI_O_AGENT_INGEST_SECRET=${IXI_O_AGENT_INGEST_SECRET}
      - PHONE_CLAW_INGEST_URL=${PHONE_CLAW_INGEST_URL}
      - PHONE_CLAW_OPENAPI_INGEST_URL=${PHONE_CLAW_OPENAPI_INGEST_URL}
      - PHONE_CLAW_BACKFILL_URL=${PHONE_CLAW_BACKFILL_URL}
      - PHONE_CLAW_INGEST_SECRET=${PHONE_CLAW_INGEST_SECRET}
      - CHANNEL_TALK_ACCESS_KEY=${CHANNEL_TALK_ACCESS_KEY}
      - CHANNEL_TALK_ACCESS_SECRET=${CHANNEL_TALK_ACCESS_SECRET}
    volumes:
      - ./n8n-data:/home/node/.n8n
      - ./n8n/workflows:/workflows:ro

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ixi-o-agent.local/schemas/miso/proposed-inbound-voice-event.schema.json",
  "title": "Proposed MISO inbound voice-session event",
  "description": "Schema proposal for a future MISO inbound event endpoint. This is not claimed as a currently supported MISO push API.",
  "type": "object",
  "required": [
    "schemaVersion",
    "eventId",
    "idempotencyKey",
    "eventType",
    "source",
    "sourceSystem",
    "sourceMode",
    "sessionId",
    "issuedAt",
    "expiresAt",
    "payloadHash",
    "startedAt",
    "endedAt",
    "summary",
    "urgency",
    "requiredTeams",
    "actionItems",
    "redactionApplied",
    "humanReviewRequired",
    "rawTranscriptIncluded",
    "rawAudioIncluded",
    "reviewStatus",
    "sourceRefs"
  ],
  "properties": {
    "schemaVersion": {
      "const": "ixi-o-agent.miso.voice-session.v0"
    },
    "eventId": {
      "type": "string",
      "description": "Globally unique event id for audit, replay detection, and tracing."
    },
    "idempotencyKey": {
      "type": "string",
      "description": "Stable key MISO can use to ignore duplicate deliveries of the same voice-session event."
    },
    "eventType": {
      "const": "voice-session.created"
    },
    "source": {
      "const": "ixi-o-agent-private-local-voice-bridge"
    },
    "sourceSystem": {
      "type": "string",
      "enum": ["channel_talk", "local_voice"]
    },
    "sourceMode": {
      "type": "string",
      "enum": ["call", "meeting", "voice_note"]
    },
    "sessionId": {
      "type": "string"
    },
    "issuedAt": {
      "type": "string",
      "format": "date-time",
      "description": "When the sender issued this event. MISO should reject stale events."
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "After this time MISO should reject the event to limit replay windows."
    },
    "payloadHash": {
      "type": "string",
      "description": "Digest of the canonical redacted payload for integrity checks before workflow execution."
    },
    "signature": {
      "type": "string",
      "description": "Optional detached signature if MISO chooses body-level signing instead of an HTTP signature header."
    },
    "startedAt": {
      "type": "string"
    },
    "endedAt": {
      "type": ["string", "null"]
    },
    "summary": {
      "type": "string"
    },
    "urgency": {
      "type": "string",
      "enum": ["unknown", "low", "normal", "high", "critical"]
    },
    "requiredTeams": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "actionItems": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["text", "owner", "status"],
        "properties": {
          "text": {
            "type": "string"
          },
          "owner": {
            "type": ["string", "null"]
          },
          "status": {
            "type": "string",
            "enum": ["open", "done", "blocked"]
          }
        },
        "additionalProperties": false
      }
    },
    "redactionApplied": {
      "const": true
    },
    "humanReviewRequired": {
      "const": true
    },
    "rawTranscriptIncluded": {
      "const": false
    },
    "rawAudioIncluded": {
      "const": false
    },
    "reviewStatus": {
      "const": "approved_for_external_workflow"
    },
    "sourceRefs": {
      "type": "object",
      "required": ["channelId", "userChatId", "callLogId", "meetMessageId"],
      "properties": {
        "channelId": {
          "type": "string"
        },
        "userChatId": {
          "type": "string"
        },
        "callLogId": {
          "type": ["string", "null"]
        },
        "meetMessageId": {
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
